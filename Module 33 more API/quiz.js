const bondCode = `My new code is: 00${7 + 1 + 2}`
// console.log(bondCode)
const data = {
    result: [{ userName: { title: 'Mr.', name: "sakib khan" } }]
}

const p = [1, 2, 3];
// const q = p.__ ? __(n => Math.pow(n, 3));

const info = { name: "", phoneNumber: null };
// console.log(JSON.stringify(info))

const array = { hobbies: ["dancing", "singing", "acting"] };
console.log(JSON.stringify(array))